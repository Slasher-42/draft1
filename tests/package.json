{
  "name": "queuecare-tests",
  "version": "1.0.0",
  "scripts": {
    "test:api": "jest --runInBand --forceExit",
    "test:ui": "npx playwright test",
    "test": "npm run test:api && npm run test:ui"
  },
  "dependencies": {
    "@playwright/test": "^1.59.1",
    "axios": "^1.6.0",
    "jest": "^29.0.0",
    "supertest": "^6.3.0"
  }
}
